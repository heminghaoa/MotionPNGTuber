# =============================================================================
# MotionPNGTuber - Requirements
# =============================================================================
#
# 重要: このプロジェクトは特定のバージョン組み合わせが必要です。
# インストール順序については README.md を参照してください。
#
# 動作確認環境:
#   - Windows 10/11
#   - Python 3.10
#   - CUDA 11.8 (GPU加速用、オプション)
#
# =============================================================================

# -----------------------------------------------------------------------------
# コア依存パッケージ (必須)
# -----------------------------------------------------------------------------

# 画像/動画処理
opencv-python==4.8.1.78
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0

# オーディオキャプチャ
sounddevice>=0.4.6

# 科学計算 (フィルタリング/スムージング用)
scipy>=1.10.0

# -----------------------------------------------------------------------------
# PyTorch (GPU または CPU)
# -----------------------------------------------------------------------------
#
# GPU版 (CUDA 11.8) - PyTorch公式サイトからインストール:
#   pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 torchaudio==2.0.2+cu118 --index-url https://download.pytorch.org/whl/cu118
#
# CPU版のみ:
#   pip install torch==2.0.1 torchvision==0.15.2 torchaudio==2.0.2 --index-url https://download.pytorch.org/whl/cpu
#
# 注意: このファイルから直接インストールしないでください - 上記のコマンドを使用してください。

# -----------------------------------------------------------------------------
# MMDetection エコシステム (anime-face-detector用)
# -----------------------------------------------------------------------------
#
# 重要: mmcv-full は pip ではなく openmim 経由でインストールする必要があります。
#
# インストール順序:
#   1. pip install openmim
#   2. mim install mmcv-full==1.7.0
#   3. pip install mmdet==2.28.0 mmpose==0.29.0
#   4. pip install anime-face-detector==0.0.9
#
openmim>=0.3.9
# mmcv-full==1.7.0  # mim経由でインストール: mim install mmcv-full==1.7.0
mmdet==2.28.0
mmpose==0.29.0
anime-face-detector==0.0.9

# -----------------------------------------------------------------------------
# オプション依存パッケージ
# -----------------------------------------------------------------------------

# 仮想カメラ出力 (OBS Studio統合用)
# pyvirtualcam>=0.10.0

# GUIのドラッグ&ドロップサポート
# tkinterdnd2>=0.3.0

# -----------------------------------------------------------------------------
# ビルド依存パッケージ (Windows)
# -----------------------------------------------------------------------------
#
# 一部のパッケージはC++コンパイルが必要です。以下が必要になる場合があります:
#   - Microsoft Visual C++ Build Tools 2019 以降
#   - ダウンロード: https://visualstudio.microsoft.com/visual-cpp-build-tools/
#   - 「C++によるデスクトップ開発」ワークロードを選択
#
